class Solution {
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        vector<vector<int>>result;
        result.push_back({});
        for(int num:nums){
            int n=result.size();
            for(int i=0;i<n;i++){
                vector<int>ns=result[i];
            ns.push_back(num);
            result.push_back(ns);
            }
            

        }
        return result;
    }
};
